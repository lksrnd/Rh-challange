<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>RH Challange</title>
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto|Varela+Round">
<link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">

  
  <body>
	<nav class="navbar navbar-default">
  <div class="container-fluid">
  <%= render 'shared/search' %>
    <div class="container">
        <div class="table-wrapper">				
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Name</th>
												<th >Matricula</th>
                        <th>Data de Nascimento</th>
												<th>Sexo</th>
                        <th>Lotação</th>
                        <th>Cargo</th>
                        <th>Ações</th>
                    </tr>
                </thead>
                <tbody>
               <tr>
                  <% @employees.each do |employee| %>
                    <td><%= employee.full_name %></td>
										<td><%= employee.id %></td>
                    <td><%= employee.birth_date %></td>
										<td><%= employee.sex %></td>
                    <td><%= employee.workspace.title %></td>
                    <td><%= employee.job_role.title %></td>
                    <td class="icons">
											<%= link_to employee, class:"show" do %>
											<i class="material-icons" title="Show">&#xE8F4;</i>
											<% end %>
											<%= link_to edit_employee_path(employee), class:"edit" do%>
											<i class="material-icons" title="Edit">&#xE254;</i>
											<% end %>
											<%= link_to employee_path(employee), method: :delete, data: { confirm: 'Você tem certeza?' }, class:"delete" do%>
											<i class="material-icons" title="Delete">&#xE872;</i>
											<% end %>
              			</td>									
          	    </tr>
					        <% end %>
                 </tbody>
            </table>
    </div>
            <div>
              <%= link_to 'Voltar', employees_path, class:"btn btn-default"  %>
            </div>
    <%= will_paginate @employees%>
	</div>
</body>
</html>


